spring:
  jpa:
    hibernate:
      ddl-auto: update # auto generate schema
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb

  liquibase:
    enabled: false